---
import DigitCounter from './DigitCounter.astro';
const t = Astro.props.t;
---

<section class="hero">
  <div class="hero-bg"></div>
  <div class="hero-content container">
    <div class="hero-text">
      <DigitCounter />
      <h1 class="hero-name">Andrés Corral</h1>
      <p class="hero-subtitle">{t['hero.subtitle']}</p>
      <p class="hero-tagline">{t['hero.tagline']}</p>
      <div class="hero-cta">
        <a href="#projects" class="btn">{t['nav.projects']} ↓</a>
      </div>
    </div>
    <div class="hero-image">
      <div class="hero-image-frame">
        <img src="/images/profile.png" alt="Andrés Corral" class="hero-img" />
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    padding-top: var(--nav-height);
  }
  .hero-bg {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(90deg, rgba(147, 51, 234, 0.06) 0%, transparent 60%),
      radial-gradient(ellipse at 80% 20%, rgba(110, 231, 183, 0.03) 0%, transparent 50%),
      radial-gradient(ellipse at 20% 80%, rgba(110, 231, 183, 0.02) 0%, transparent 50%);
    pointer-events: none;
  }
  .hero-content {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 80px;
    align-items: center;
  }
  .hero-text {
    max-width: 700px;
  }
  .hero-image {
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .hero-image-frame {
    position: relative;
    width: 320px;
    height: 320px;
    overflow: hidden;
    border: 1px solid var(--color-border);
  }
  .hero-image-frame::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(147, 51, 234, 0.1), transparent 50%);
    z-index: 1;
    pointer-events: none;
  }
  .hero-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  .hero-name {
    font-family: var(--font-heading);
    font-size: clamp(48px, 10vw, 140px);
    font-weight: 400;
    line-height: 1;
    margin: 24px 0 16px;
    color: var(--color-text);
  }
  .hero-subtitle {
    font-size: clamp(14px, 2vw, 18px);
    color: var(--color-muted);
    line-height: 1.5;
    margin-bottom: 16px;
  }
  .hero-tagline {
    font-size: clamp(13px, 1.5vw, 15px);
    color: var(--color-muted-strong);
    line-height: 1.6;
    max-width: 600px;
    margin-bottom: 40px;
  }
  .hero-cta {
    display: flex;
    gap: 16px;
  }
  @media (max-width: 768px) {
    .hero-content {
      grid-template-columns: 1fr;
      gap: 40px;
    }
    .hero-image {
      order: -1;
    }
    .hero-image-frame {
      width: 200px;
      height: 200px;
    }
  }
</style>
